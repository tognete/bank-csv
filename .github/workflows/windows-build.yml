name: windows-build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install pyinstaller

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Build frontend
        run: npm run build
        working-directory: frontend

      - name: Install OCR binaries (Chocolatey)
        shell: pwsh
        run: |
          choco install tesseract -y --force
          choco install poppler -y --force
          New-Item -ItemType Directory -Path vendor -Force | Out-Null
          New-Item -ItemType Directory -Path vendor\tesseract -Force | Out-Null
          Copy-Item -Recurse -Force "C:\Program Files\Tesseract-OCR\*" "vendor\tesseract\"

          $popplerPkg = Get-ChildItem "C:\ProgramData\chocolatey\lib" -Filter "poppler*" -Directory | Select-Object -First 1
          if (-not $popplerPkg) { throw "Poppler Chocolatey package not found." }
          $popplerTools = Get-ChildItem "$($popplerPkg.FullName)\tools" -Directory | Select-Object -First 1
          if (-not $popplerTools) { throw "Poppler tools folder missing inside chocolatey package." }
          New-Item -ItemType Directory -Path vendor\poppler -Force | Out-Null
          Copy-Item -Recurse -Force "$($popplerTools.FullName)\*" "vendor\poppler\"

      - name: Build Windows executable
        run: pyinstaller packaging/windows/bank_csv.spec --noconfirm

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: BankCSV
          path: dist/BankCSV/
